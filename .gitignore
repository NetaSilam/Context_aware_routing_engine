.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
.mypy_cache/
.ruff_cache/

.env

node_modules/
dist/
*.tsbuildinfo

# Seed dump can be large; keep it out of git history until we pick a storage
# location for it (course-provided storage, a release asset, etc.).
db/seed/*.dump

.DS_Store
Thumbs.db
