name: grading-validation

on:
  push:
  pull_request:

jobs:
  validation:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Run isolated grading validation
        run: ./scripts/run_grading_validation.sh
