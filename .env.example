# Copy to .env and fill in real values. Never commit the real .env file.

# The next foundation ticket will define the validated application, PostGIS,
# Redis, and testing configuration. This cleanup milestone has no Compose stack.

# LLM (pick whichever provider you use)
GEMINI_API_KEY=your_secret_api_key_here
OPENAI_API_KEY=your_secret_api_key_here
HUGGINGFACE_API_KEY=your_secret_api_key_here
